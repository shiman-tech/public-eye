# Frontend (React) — safe to expose in the browser
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: override API base URL (defaults to /api via Vite proxy in dev)
# VITE_API_URL=http://localhost:8000/api
